% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_venn_de}
\alias{plot_venn_de}
\title{Create Venn Diagram for DE Gene Overlap}
\usage{
plot_venn_de(
  results_df,
  log2FC_threshold = 1,
  padj_threshold = 0.05,
  direction = "both",
  methods = NULL,
  colors = NULL,
  title = "DE Gene Overlap",
  return_type = "plot"
)
}
\arguments{
\item{results_df}{Data frame containing DE results with columns:
\code{method}, \code{gene_id}, \code{log2FC}, \code{padj}}

\item{log2FC_threshold}{Log2 fold change threshold for significance (default: 1)}

\item{padj_threshold}{Adjusted p-value threshold for significance (default: 0.05)}

\item{direction}{Direction to filter: "up" (upregulated), "down" (downregulated),
or "both" (default: "both")}

\item{methods}{Vector of method names to include (default: NULL, uses all methods)}

\item{colors}{Vector of colors for sets (optional)}

\item{title}{Plot title (default: "DE Gene Overlap")}

\item{return_type}{Return "plot" for visualization or "sets" for gene lists (default: "plot")}
}
\value{
Either a ggplot object (return_type = "plot") or a list of gene sets
  (return_type = "sets")
}
\description{
Generates Venn diagrams showing overlap of differentially expressed genes
across multiple methods. Supports 2-way and 3-way comparisons.
}
\examples{
# Create example results with multiple methods
set.seed(123)
results_df <- data.frame(
  method = rep(c("DESeq2", "edgeR", "limma-voom"), each = 100),
  gene_id = rep(paste0("Gene", 1:100), 3),
  log2FC = rnorm(300, 0, 2),
  padj = runif(300)
)

# 3-way Venn diagram
p <- plot_venn_de(results_df, log2FC_threshold = 0.5, padj_threshold = 0.1)
print(p)
}
