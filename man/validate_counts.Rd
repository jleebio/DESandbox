% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_validation.R
\name{validate_counts}
\alias{validate_counts}
\title{Validate Count Matrix}
\usage{
validate_counts(
  counts,
  min_counts = 10,
  min_samples = 3,
  allow_negative = FALSE
)
}
\arguments{
\item{counts}{A numeric matrix or data.frame with genes in rows and samples in columns}

\item{min_counts}{Minimum total counts per gene (default: 10)}

\item{min_samples}{Minimum number of samples a gene must be expressed in (default: 3)}

\item{allow_negative}{Whether to allow negative values (default: FALSE)}
}
\value{
A list with validation status and cleaned count matrix
}
\description{
Validates that the count matrix meets requirements for DE analysis.
}
\examples{
counts <- matrix(rpois(1000, 10), nrow=100, ncol=10)
result <- validate_counts(counts)
}
